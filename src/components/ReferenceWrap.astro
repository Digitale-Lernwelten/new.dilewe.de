---
interface Props {
	image?: ImageMetadata;
	odd?: boolean;
}

import Reference from './Reference';
const {image, odd} = Astro.props;

---

<Reference image={image} odd={odd ? true : false} client:only>
	<slot />
</Reference>
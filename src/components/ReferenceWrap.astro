---
interface Props {
	images: ImageMetadata[];
	imageAlt: string;
	odd?: boolean;
}

import Reference from './Reference';
const {images, imageAlt, odd} = Astro.props;

---

<Reference images={images} imageAlt={imageAlt} odd={odd ? true : false} client:only="react">
	<slot />
</Reference>

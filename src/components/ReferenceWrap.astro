---
interface Props {
	images: ImageMetadata[];
	odd?: boolean;
}

import Reference from './Reference';
const {images, odd} = Astro.props;

---

<Reference images={images} odd={odd ? true : false} client:only="react">
	<slot />
</Reference>

---
import AboutUsHeaderCard from './AboutUsHeaderCard'

interface Props {
	image?: ImageMetadata;
	alt?: string;
	odd?: boolean;
}

const {image, alt, odd} = Astro.props;

---

<AboutUsHeaderCard image={image} alt={alt} odd={odd ? true : false} client:only="react">
	<slot />
</AboutUsHeaderCard>